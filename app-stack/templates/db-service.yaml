apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: database
spec:
  selector:
    app: db
  ports:
    - port: {{ .Values.database.port }}
      targetPort: {{ .Values.database.port }}